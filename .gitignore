# Python
__pycache__/
*.py[cod]
.pytest_cache/
.pytest_tmp/
.ruff_cache/
.mypy_cache/
.venv/
.uv-verify/
.uv-cache/
venv/
venvs/

# Local secrets and env files
config.override.toml
my_config*.toml
*.local.toml
.env
*.env
# Must stay last: a later ignore pattern re-excludes what an earlier `!` re-included.
!.env-example
*API.txt
api_env.txt

# AI assistants
CLAUDE.md
CLAUDE.zh-CN.md
AGENTS.md

# Generated artifacts
outputs/
anchor_bank/
# Defensive: runtime data must never be committed.
data/
# Read-only pipeline *input* (ontology definition + pre-computed embeddings).
# Tracked on purpose: the Docker build copies it, and a clean clone cannot
# regenerate it without an embedding API. See README "Prerequisites".
!ontology/
!ontology/*.json
*.log
run.stdout.log
run.stderr.log

# IDE
.idea/
.vscode/

# Build artifacts
build/
dist/
*.egg-info/

# Local/temp workspace
.local/

# OS files
.DS_Store
Thumbs.db
